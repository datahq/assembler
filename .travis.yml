sudo:
  required

services:
    - elasticsearch

dist:
  trusty

language:
  python

python:
  - 3.6

env:
  global:
    - TOXENV="py${PYTHON_VERSION//./}"

install:
  - make install
  - pip install coveralls

script:
  - moto_server & make test

before_script:
  - sleep 30
  - curl localhost:9200

after_success:
  - coveralls
